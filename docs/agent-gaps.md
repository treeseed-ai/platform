# Agent architecture implementation gaps

Temporary working list for local code and contract closure before the golden acceptance campaign. Remove an item only after its owning implementation and focused unit/integration tests pass. The architecture plans and `agent-acceptance.md` retain the full end-to-end gates; this file is not another acceptance authority.

- [ ] **Living-graph conditions (SDK/API/TreeDX).** Typed exact `depends_on` links are merged in TreeDX staging (PR #65). The API locally projects complete drafts, runnable proposal review, exact-source feedback and decision conditions, and proposed Actor–Reviewer work. Reviewer decisions and resolution messages receive exact TreeDX read-back; missing-source and database-only resolutions fail closed. Draft → review → resolution → decision → acting and blocker reopening pass local graph-state tests; 1,098 control-plane tests and the API build pass. The broader lifecycle/external/approval conditions and PostgreSQL admission transition still need localized closure. Live TreeDX/API read-back belongs to the acceptance campaign.
- [ ] **Schema equivalence and content migration (SDK/API/TreeDX/Platform).** SDK verification detects top-level field, required-field, and constant drift for all five target models; strict nested identifier, slug, exact-reference, uniqueness, Book-page repinning, and Discussion authoring tests pass. The retired allocation migration now safely settles orphaned workdays and the local database cutover has no pending or unexpected migrations. Exhaustive nested declaration verification, remaining consumers, and governed migration of existing library content remain. Managed read-back belongs to acceptance.
- [ ] **Architect knowledge convention (TreeDX/Agent).** Profile inspection and project-owned handler build/selection have focused tests. Architect acting now requires a validated Knowledge page bound to the exact conventional `<Project> Architecture` Book context and ordinary AgentKernel TreeDX commit path; focused prompt, success, and fail-closed tests pass. Existing project libraries still need their conventional Book/page content migrated and read back before this gap can close.
