# Agent architecture implementation gaps

Temporary working list for local code and contract closure before the golden acceptance campaign. Remove an item only after its owning implementation and focused unit/integration tests pass. The architecture plans and `agent-acceptance.md` retain the full end-to-end gates; this file is not another acceptance authority.

- [ ] **Living-graph conditions (SDK/API/TreeDX).** Typed exact `depends_on` links are merged in TreeDX staging (PR #65); SDK/API focused tests, PostgreSQL graph/admission/settlement tests, bounded review exhaustion, and cross-project endpoint-grant denial pass. The graph does not yet project governed blocking questions/feedback and authority prerequisites as condition nodes with authoritative state resolution. A live TreeDX/API read-back belongs to the subsequent acceptance campaign.
- [ ] **Schema equivalence and content migration (SDK/API/TreeDX/Platform).** SDK/API/CLI execution contracts and clean PostgreSQL migration/admission/settlement tests pass, but `docs/agent.schema.yml` lacks an automated structural-equivalence gate. Its Book, Knowledge, Objective, Discussion, and DiscussionMessage shapes differ from persisted TreeDX content. Migrate those models to the plan's exact-reference shapes while preserving existing knowledge slug, order, visibility, and publication features; update consumers and existing content, then verify the cutover without compatibility aliases. Managed host migration belongs to acceptance.
- [ ] **Architect knowledge convention (TreeDX/Agent).** Profile inspection and project-owned handler build/selection have focused tests. The Architect must maintain the conventional `<Project> Architecture` book through ordinary governed TreeDX operations; its prompt/content contract and localized verification remain unproven. The local API is currently HTTP 502 because its container reports `API_ENTRYPOINT_MIGRATIONS_FAILED`; do not treat empty TreeDX reads as authoritative.
