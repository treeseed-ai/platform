schemaVersion: treeseed.platform-profile/v1
id: treeai
extends: []
sources: { projects: [sdk, cli, identity, api, admin, agent, reviewer, treedx, deployment] }
runtime:
  targets: [treeai.inference, treeai.training, treeai.lab]
  defaultEnabled: false
